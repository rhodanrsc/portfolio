import React from "react";

const Contact = (props) => {
    return(
        <div id="Contact" className="contact snap" ref={props.refp}>
            <h3 >Contact</h3>

            
        </div>
    );
}

export default Contact;